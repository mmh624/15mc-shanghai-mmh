# 15mc-shanghai-mmh


# install / upgrade if needed
import subprocess, sys
subprocess.run([sys.executable, "-m", "pip", "install", "-q",
                "h3", "folium", "pyproj", "shapely", "geopandas",
                "numpy", "pandas", "matplotlib"])
I use jupyter,install the moudles.
